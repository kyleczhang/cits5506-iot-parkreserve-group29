# Leave empty in local dev so the browser talks to Vite on :5173 and Vite
# proxies `/api` + `/socket.io` to the backend. Only set this when you
# intentionally want the browser to call the backend origin directly.
VITE_BACKEND_ORIGIN=
